# 專案規則（Claude Code 適用）

## 變更程式碼後的固定流程

每次變更程式碼後，務必依序完成以下兩件事：

1. **加上程式碼註解**：在變更的地方加上簡短註解，說明這段程式碼在做什麼、為什麼這樣寫（尤其是不容易一眼看懂的部分），方便使用者學習與日後回顧。
2. **記錄到 `claude_core_assistant.md`**：在專案根目錄的 `claude_core_assistant.md` 補上一筆紀錄，包含：
   - 變更的背景/原因
   - 實際做了什麼變更（含檔案路徑）
   - 重要的取捨或決策說明

這兩步驟是固定流程，不需要使用者每次額外提醒。

## 設計新區塊時的固定流程

規劃一個新區塊（例如 #skills、#portfolio）時，依循以下五階段進行，不需使用者每次重新說明：

1. **內容架構規劃優先**：先分析「必要資訊 vs. 補充資訊」，盡可能詳細列出欄位與理由，不要一開始就跳去做視覺/排版。
2. **只出規格文件，不動程式碼**：產出 `docs/superpowers/specs/日期-區塊名稱-design.md`，內容包含現況盤點、必要/補充資訊分級、Governance 決策。文件結尾固定加一段「實作前必須確認的技術選型清單」（例如：要不要引入 JS、要不要拆成獨立頁面、篩選/互動要做到多細），讓使用者確認規格文件內容時一併回答，避免進入實作階段又要重新開一輪釐清。
3. **等待明確授權才進入實作，且先做核心版本**：使用者確認規格文件後才落地 HTML/SCSS，預設「先做核心/demo 版本」，不是規格文件列的所有欄位一次到位。若使用者的實作指令帶有具體可量化的版型描述（例如「卡片固定 300×560px」），或明確只有一種合理實作方式，可以直接照做。

   **但只要排版/結構類指令存在超過一種合理解讀（例如「排成一列 3 欄」可能是同分類內部格線、也可能是跨分類合併成一份清單），一律先提供 2–3 款具體範例讓使用者選定方向後才動手實作，不要自己選一種直接做完、事後才用截圖跟使用者確認。** 這比「先用文字描述方向」更進一步：純文字描述容易被略讀，使用者未必真的意識到這裡有分歧點；具體範例（用 AskUserQuestion 附 preview 手繪示意圖，或至少用文字清楚描述每款的差異與畫面）才能逼自己先想清楚有哪些合理選項、也才能讓使用者真正在動手前比較、排除歧義。曾經因為跳過這一步，直接選一種實作，結果做錯方向、使用者要求「回到上一個 commit」整個丟棄重做——這個代價遠高於先花時間問清楚。
4. **遇到高歧義詞彙，主動複述理解，不要自己假設**：已知的高歧義詞（隨時間增補）：
   - 「並排」：可能是同一分類內部的多欄格線，也可能是跨分類配對成兩欄。
   - 「分頁」：可能是頁內錨點跳轉，也可能是獨立的 HTML 檔案。
   - 「篩選器」：可能是純 CSS 技巧，也可能要引入 JavaScript。
   - 「卡片」：本站預設不做卡片（貼版面留白），除非使用者明確要求。

   這些詞出現在指令裡時，動手前先用一句話複述「我理解成 A」，讓使用者能在**動手前**確認或糾正，而不是做完才發現理解錯誤。
5. **交付前先自我審查，不要等使用者事後挑錯**：實作完成、用 Playwright 截圖驗證功能正常後，交付前再檢查一輪常見盲點：卡片/欄位是否等寬等高、有無橫向溢出（尤其手機寬度）、跟既有區塊的間距/字級是否一致、RWD 是否測過中間寬度（不是只測最寬與最窄兩極）。

之後才會是使用者主動要求 commit/push，通常會一次提交整個 session 累積的變更，不需要每完成一個小階段就主動詢問要不要 commit。

## Git commit 訊息語言

commit message 一律使用**繁體中文**撰寫，不要用英文。

## 預設使用的 Skill

本專案預設使用以下五個 skill，不需要使用者每次額外提醒或指定：

- **frontend-design**：視覺設計方向指引，避免落入樣板感的配色、字體、排版
- **frontend-design-direction**：更完整的產品級前端設計方向設定
- **brainstorming**：任何創意/新功能/樣式改動前，先釐清需求與取捨
- **browser-use**：實際打開瀏覽器測試效果、抓截圖驗證
- **adversarial-ux-test**：完成後模擬挑剔使用者操作，抓出真正值得修的 UX 問題
