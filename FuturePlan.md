# 未來規劃

> 本文件記錄專案目前**不急著做、但值得先寫下來**的規劃方向，包含雙語頁面實作方式、顏色變數系統演進、多頁面架構等，待條件成熟（例如網站基本雛形完成、導入建置工具）再回頭啟動。

---

# 規劃一：雙語頁面（中文／英文）實作方式

> **待網站基本雛形（頁面結構、基礎內容、基礎樣式）完成後再啟動**。目前請優先完成 `index.html` 的內容與頁面骨架，不要現在就開始雙語相關的程式碼變更。

## 專案現況摘要（規劃時的基準）

- 純靜態單頁網站，僅 `src/pages/index.html` 一支頁面，用錨點（`#about`／`#works`／`#contact`）分區。
- **完全沒有 JavaScript、沒有建置工具**（`sass` 需手動執行編譯 SCSS，無 watch、無 npm script）。
- **沒有任何 i18n／多語言相關程式碼或痕跡**，是從零開始的規劃。
- 既有瑕疵：`<html lang="en">` 但內文全是繁體中文，語言標記與實際內容不符，未來做雙語化時應一併修正。
- 文案目前全部寫死在 HTML 裡，沒有從 JSON/資料檔載入的機制。

## 四種候選方案比較

### 方案 A：純靜態複製頁面（雙檔案／雙目錄）

**原理**：中英文各自維護一份完整獨立的 HTML，例如 `index.html`（中）+ `index.en.html`（英），或走子目錄 `/zh/index.html`、`/en/index.html`。語言切換 = 一個普通連結，導到另一個網址。

| 面向 | 評估 |
|---|---|
| SEO | ★★★★★ 最佳。各語言各自獨立網址、正確 `lang` 屬性，加上 `hreflang` 互相標註即可，搜尋引擎最容易正確索引。 |
| 維護／擴充 | ★★☆☆☆ 最差。文案分散在兩份 HTML，改一處文字要記得改兩份，容易漏改、不同步；頁面一多，維護成本線性上升。 |
| 使用者體驗 | ★★★☆☆ 普通。切換語言是整頁換頁，有網址變化與重新載入，沒有平滑過場，但技術最單純、不會有「未翻譯內容閃爍」問題。 |
| 技術複雜度／相容性 | 最低，不需要 JS、不需要建置工具，跟現況完全相容，可立即動手。 |

### 方案 B：JS 動態切換（單頁 + JSON 語言字典）

**原理**：只維護一份 HTML，文字節點用 `data-i18n="key"` 之類的屬性標記，另外寫 `zh.json`／`en.json` 語言包，由 JavaScript 在載入或使用者點擊切換鈕時，讀取 JSON 並替換 DOM 文字內容（網址通常不變，用 `localStorage` 記住使用者選擇）。

| 面向 | 評估 |
|---|---|
| SEO | ★☆☆☆☆ 最差。搜尋引擎爬到的是伺服器回應的預設語言內容（JS 尚未執行），另一語言版本實質上不會被獨立索引到；沒有獨立網址可分享／收藏特定語言版本。 |
| 維護／擴充 | ★★★★☆ 良好。文案集中在 JSON 字典，改文字只需改一處；新增語言只要多加一份字典檔，HTML 結構完全不用動。 |
| 使用者體驗 | ★★★★☆ 良好。切換語言不用整頁重載、無網址跳轉，感覺最現代化；缺點是若沒處理好，首次載入可能有短暫「先顯示 A 語言再閃成 B 語言」的畫面。 |
| 技術複雜度／相容性 | 最高衝擊。這是專案第一次引入 JavaScript，且需要設計字典結構、切換邏輯、狀態記憶機制，等於從零建立一整套小型 i18n 系統。 |

### 方案 C：資料驅動 + 建置時產生靜態頁面（建議方向）

**原理**：把文案抽成一份語言中立的資料檔（YAML/JSON，例如 `content/zh.json`、`content/en.json`）+ 共用樣板，寫一支 Node 建置腳本，在「建置時」把樣板套資料，產生出 `dist/zh/index.html`、`dist/en/index.html` 兩份純靜態 HTML。上線的仍然是純靜態檔案，瀏覽器端完全不需要 JS。

| 面向 | 評估 |
|---|---|
| SEO | ★★★★★ 最佳。跟方案 A 一樣是各語言獨立網址、伺服器直接吐出正確語言內容，SEO 表現相同。 |
| 維護／擴充 | ★★★★★ 最佳。文案與樣板分離，改文字只改資料檔一處，樣板只維護一份；加新頁面或新語言都只是加資料、跑一次建置指令，不會有方案 A 那種「複製貼上兩份」的同步風險。 |
| 使用者體驗 | ★★★☆☆ 跟方案 A 相同（整頁換頁），除非額外疊加 JS 做平滑轉場。 |
| 技術複雜度／相容性 | 中高。需要新增 Node 建置腳本、簡單樣板語法（不必上框架）、以及一個「建置」步驟（可與現有的 `sass` 編譯步驟合併成一個腳本）。不需要瀏覽器端 JS。 |

### 方案 D（不建議）：CSS-only 顯示／隱藏切換

**原理**：兩種語言文字都寫在同一個標籤內，用 CSS 依 `html[lang]` 或 class 切換顯示／隱藏其中一份。

列出只是為了完整比較：SEO 差（爬蟲會讀到兩種語言文字擠在一起，或被隱藏文字誤判為隱藏作弊內容）、維護差（中英文穿插寫在同一標籤，可讀性很差）、無障礙差（螢幕報讀器可能唸出被 CSS 隱藏的文字）。**不建議採用**。

## 建議方向

三個優先面向（SEO、維護擴充、體驗）中，**SEO 與「網址不變的絲滑切換」在技術上天生互斥**——要讓搜尋引擎正確索引兩種語言，最終一定要有兩個獨立網址；要做到真正網址不跳轉的切換，語言差異就只能藏在同一份文件裡，SEO 必然打折。

因此建議採用**方案 C 為地基，體驗面用「漸進增強」補強**，而非追求網址完全不變：

1. **地基：方案 C（資料驅動 + 建置時產生靜態頁面）**
   - 滿足 SEO（獨立網址、正確 `lang`／`hreflang`）與維護擴充（文案與樣板分離，未來加頁面、加語言只需加資料）。
   - 沿用專案既有的 7-1 SCSS 紀律與 BEM 命名，樣板本身結構不變，只是文字來源從「寫死」改成「從資料檔套版」。
   - 建置腳本可以和現有的 `sass` 編譯指令合併成一個 `npm run build`，順便解決 `src/package.json` 目前是空檔案、沒有 script 的既有缺口。

2. **體驗補強（可選，非必要）**：語言切換連結維持是「換到另一個獨立網址」這個本質（SEO 不犧牲），但可以疊加輕量、非必要的視覺過場（例如簡單的 CSS/View Transitions 淡入淡出，或極少量 JS 做預抓取讓切換「感覺」更快），而不是用 JS 動態替換文字。這一層完全是加分項，缺席也不影響網站可用性。

**不建議方案 A**（純複製兩份 HTML）：未來維護與擴充容易度是明確在意的面向，而方案 A 正是在這一項評分最差、且風險是「兩份文案不同步」這種容易被忽略又難排查的問題；方案 C 幾乎是方案 A 的「同樣 SEO 效果、但沒有同步風險」的正式升級版，多付出的建置腳本成本是一次性的。

**不建議方案 B**（純 JS 動態切換）：SEO 是明確在意的面向，而方案 B 在 SEO 這項是四個方案中最弱的，且是專案第一次引入 JavaScript 卻只換來體驗加分、卻要用 SEO 換，優先順序上不划算。若之後體驗優先度大幅提高於 SEO，可重新考慮方案 B，或把它疊加在方案 C 之上做「同網址內的語言記憶＋預設語言判斷」增強。

## 若未來採用建議方案，預期會動到的檔案／資料夾

- 新增 `src/content/zh.json`、`src/content/en.json`（或 YAML）存放頁面文案。
- 新增一支 Node 建置腳本（例如 `scripts/build.js`），讀資料檔套用 HTML 樣板，輸出到各語言子目錄；可與 `sass` 編譯步驟合併，並在 `src/package.json` 補上 `scripts`（目前是空檔案）。
- 現有 `src/pages/index.html` 改造成「樣板」（文字改成佔位符／簡單樣板語法），不再是最終輸出的頁面本身。
- 修正既有的 `<html lang="en">` 與內容語言不一致問題——樣板產出時依語言正確設定 `lang` 屬性，並互相加上 `hreflang` 標籤。
- 每個語言版本頁面加上互相連結的語言切換連結。
- 依專案 `CLAUDE.md` 規定：改動後補程式碼註解、並在 `claude_core_assistant.md` 記錄本次變更的背景與取捨。

## 前置條件

**此規劃需等網站基本雛形（頁面結構、基礎內容、基礎樣式）完成後才啟動。** 目前優先順序是先把 `index.html` 的內容與頁面骨架做完整，雙語化留待之後再回頭執行本文件的規劃。

## 驗證方式（實作啟動後）

- 用靜態伺服器分別開啟兩語言版本頁面，確認文案、`lang` 屬性、`hreflang` 標籤皆正確。
- 檢查建置腳本重跑後兩語言頁面文案是否同步（改一處資料檔，兩語言各自對應內容應正確反映，不需要手動複製）。
- 用瀏覽器開發者工具或線上 SEO 檢測工具確認語言標記與 `hreflang` 設定正確可被解析。

---

# 規劃二：顏色變數系統演進（Sass `$變數` vs CSS 自訂屬性 `--變數`）

## 背景

專案顏色目前集中放在 `src/styles/abstracts/_variables.scss`，用 Sass 變數（`$color-platinum` 等）管理，各 SCSS 檔案 `@use` 進來直接引用。這是「編譯期」變數：`npx sass` 編譯時就會被展開成固定值寫進 `main.css`，瀏覽器執行期看不到變數名稱、也無法動態改變。

CSS 另外還有一種原生的「執行期」變數——**CSS 自訂屬性**（`--變數`，用 `var(--變數)` 取值）。兩者定位不同，不是二選一的替代關係。

## 比較

| | `$變數`（Sass） | `--變數`（CSS 自訂屬性） |
|---|---|---|
| 解析時機 | 編譯時展開成固定值，`main.css` 看不到變數名稱 | 瀏覽器執行期解析，`main.css` 保留 `var(--xxx)` |
| 能否被 JS／media query／使用者互動動態改變 | 不行 | 可以（`element.style.setProperty`、深色模式、主題切換都靠它） |
| Cascade／繼承 | 沒有，純粹是文字取代 | 有，跟一般 CSS 屬性一樣層疊、繼承，可在特定元件內局部覆寫 |
| 運算/邏輯能力 | 強，能配合 Sass 的 `map`、`@function`、`@if` | 弱，只能配合 `calc()`，做不了條件判斷 |

## 目前結論

**維持現況，繼續用 `$變數`（Sass）即可**，不需要現在導入 CSS 自訂屬性。理由：目前是純靜態小型個人網站，沒有深色模式、使用者主題切換、或 JS 動態改色的需求，`$變數` 跟現有 7-1 架構（`abstracts/_variables.scss` 集中管理）完全契合，`main.css` 輸出也乾淨，不必預先增加複雜度。

## 未來觸發條件（出現以下任一種需求時，才需要導入 `--變數`）

- 想做**深色模式**（用 `prefers-color-scheme` 或切換按鈕改變 `:root` 上的 `--color-*`）
- 想讓某個元件**局部覆寫**顏色（例如某個 section 背景要跟全站不同，CSS 變數的 cascade 特性最方便處理）
- 想用 **JS** 動態調色

## 屆時建議做法（先寫下來，現在不用做）

兩者並存，而非二選一：`$變數` 仍是唯一真實來源（給 Sass 的運算/map 用），在 `:root` 選擇器內把需要動態化的顏色 `--color-platinum: #{$color-platinum};` 輸出成對應的 CSS 自訂屬性，實際樣式規則改引用 `var(--color-platinum)`。這樣兩邊優點都拿到：Sass 端仍可用變數做編譯期邏輯，執行期又保留可被 JS/媒體查詢動態覆寫的彈性。

---

# 規劃三：作品集（及未來可能的部落格/筆記）拆成獨立分頁

## 背景

規劃大學生版五大核心＋四個加分項內容時，使用者提出「作品集可以規劃成一個獨立分頁」的想法。目前網站是**純單頁錨點架構**（`src/pages/index.html` 一支檔案，`#portfolio`、`#notes` 等都只是頁面內的錨點區塊），完全沒有建置工具、沒有樣板系統。若現在就把作品集拆成獨立的 `portfolio.html`，`site-header`／`site-nav` 等共用區塊必須手動複製一份到新檔案，之後改一處（例如 header 改版、新增導覽項目）都要記得同步改兩份，跟先前 [[雙語頁面（中文／英文）實作方式|規劃一]] 討論「純靜態複製 vs 資料驅動生成」時分析的維護風險是同一個問題。

使用者確認：**這次先不拆頁，作品集（以及結構上很類似的「學習筆記」`#notes`——部落格類內容通常也需要「列表頁＋各篇獨立頁」，錨點模式並不適合）維持在首頁錨點區塊**，分頁的想法先記錄在這裡，等以後有需要（內容多到單頁滾動不合理、或想要作品/筆記各自有獨立網址可分享）再啟動。

## 未來觸發條件

- 作品集項目多到讓首頁過長、使用者滾動體驗變差
- 想讓單一作品或單篇筆記有自己的網址可以分享（例如求職時只想附上某個作品的連結，而不是整個首頁）
- 學習筆記／部落格內容量變大，需要「列表頁＋各篇內文頁」的結構，錨點模式無法呈現

## 建議做法（先寫下來，現在不用做）

比照 [[雙語頁面（中文／英文）實作方式|規劃一]] 的結論：**不要用純手動複製 HTML 的方式拆頁**（維護風險高，header/nav 改一次要記得改好幾份），而是等專案準備好引入資料驅動 + 建置時產生靜態頁面（同一支 Node 建置腳本，也能同時處理雙語與多頁，兩個需求可以合併在同一次建置工具導入時一起解決）之後，再把作品集、學習筆記等內容拆成獨立頁面。屆時作品集可能的頁面切法：`index.html`（首頁精選幾件代表作 + 連結到完整作品集頁）＋ `portfolio.html`（完整作品集列表）；學習筆記類似處理成列表頁＋各篇獨立頁。

## 2026-07-23 決策異動：作品「詳細專案說明」提前手動拆頁

規劃 `#portfolio` 卡片集＋篩選器時，使用者明確要求作品卡片點擊後要能「跳轉到同一網站、不同 HTML 檔案」的詳細專案說明頁，不等建置工具就緒。已明確告知使用者這正是上面分析過的「純手動複製 HTML」風險（header/nav/footer 要手動同步維護），使用者選擇接受這個代價，先做示範。

**目前狀態**：只有 `src/pages/portfolio/project-1.html` 這 1 支手動複製的詳細頁面（header/nav/footer 從 `index.html` 複製，nav 錨點改成 `/src/pages/index.html#xxx` 絕對路徑）。之後每新增一支詳細頁都要重複這個手動複製流程；若之後真的要導入建置工具（資料驅動＋樣板產生），屆時需要遷移的手動頁面數量以這裡的紀錄為準，逐支確認遷移完成。
